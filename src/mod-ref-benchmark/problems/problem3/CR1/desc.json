{
  "source": "modref",
  "name": "Bus Driver Scheduling – CR1",
  "content": "Modify the coverage constraint so that each task must now be covered exactly two times instead of exactly once. A shift may still be selected only once. The objective remains minimizing the number of selected shifts.",
  "value_info": [
    "`n_tasks`: The total number of tasks to be covered.",
    "`shifts`: A list of shifts, where each shift is represented as a list of task indices that it covers."
  ],
  "ref_sol_format": {
    "var1": {
      "descr": "`x`: A list of 0/1 values of length `nShifts`, where `x[i] = 1` if shift i is selected.",
      "example": "[0,1,0,1]",
      "type": "List[int]",
      "size": "[len(shifts)]",
      "index": "0-based"
    }
  },
  "prob_type": "cop",
  "complexity": {
    "difficulty_label": "low",
    "difficulty_rationale": "Only changes the target cardinality of the existing task-coverage constraints from one covering shift to two.",
    "change_types": {
      "constraints": "modify",
      "decision_variables": "none",
      "objective": "none"
    },
    "base_model": {
      "decision_variable_count": 2503,
      "constraint_count": 53
    },
    "cr_impact": {
      "affected_decision_variables": 2503,
      "affected_constraints": 53,
      "notes": "Variable families: x. Constraint groups: task_coverage_cardinality."
    }
  }
}
