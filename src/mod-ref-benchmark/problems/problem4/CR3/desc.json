{
  "source": "modref",
  "name": "Warehouse Location – CR3",
  "content": "Warehouses may optionally operate in an expanded mode. In expanded mode, a warehouse can serve more stores but requires paying an additional upgrade fee. The decision of whether to operate a warehouse normally or in expanded mode is part of the optimization. All other servicing rules apply as before.",
  "value_info": [
    "`fixed_cost`: Integer fixed cost for opening a warehouse.\n`capacities`: List of normal capacities per warehouse.\n`capacity_expanded`: List of expanded capacities per warehouse (must exceed capacities).\n`upgrade_cost`: Additional cost for enabling expanded mode on each warehouse.\n`supply_cost`: A 2D matrix where supply_cost[i][j] is the cost of assigning store i to warehouse j."
  ],
  "ref_sol_format": {
    "var1": {
      "descr": "`w`: A list of length n_stores, where w[i] is the index of the warehouse assigned to store i.",
      "example": "[0,2,2,4,4,1,1,2,3,4]",
      "type": "List[int]",
      "size": "[n_stores]"
    },
    "var2": {
      "descr": "`o`: A list of binary values of length n_warehouses, where o[j] = 1 if warehouse j is open.",
      "example": "[1,0,1,0,1]",
      "type": "List[int]"
    },
    "var3": {
      "descr": "`upgrade`: A list of binary values of length n_warehouses, where upgrade[j] = 1 if warehouse j uses the expanded capacity.",
      "example": "[0,1,0,0,1]",
      "type": "List[int]"
    },
    "var4": {
      "descr": "`total_cost`: The total objective value after considering supply costs, fixed warehouse opening costs, and expansion costs.",
      "example": "542",
      "type": "int"
    }
  },
  "prob_type": "cop",
  "complexity": {
    "difficulty_label": "high",
    "difficulty_rationale": "Adds a new upgrade decision family and rewrites capacity handling so warehouse capacity becomes endogenous, with matching cost changes in the objective.",
    "change_types": {
      "constraints": "modify",
      "decision_variables": "add",
      "objective": "modify"
    },
    "base_model": {
      "decision_variable_count": 25,
      "constraint_count": 25
    },
    "cr_impact": {
      "affected_decision_variables": 15,
      "affected_constraints": 5,
      "notes": "Variable families: w and u. Constraint groups: warehouse_capacity_with_optional_upgrade. Objective change: modify."
    }
  }
}
